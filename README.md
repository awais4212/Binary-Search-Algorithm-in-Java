# Binary-Search-Algorithm-in-Java
This Java program implements the Binary Search algorithm on a sorted array. It searches for a key by repeatedly dividing the array in half, checking the middle element, and adjusting the search range. If the key is found, its index is displayed; otherwise, a message shows it is not present.
